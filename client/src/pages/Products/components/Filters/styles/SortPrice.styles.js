import styled from "styled-components";

export const Container = styled.div`
  select {
    padding: 5px 10px;
    border: 1.33px solid red;
    font-size: 1rem;
    border-radius: 4px;
    user-select: none;
    outline: none;
  }
`;
